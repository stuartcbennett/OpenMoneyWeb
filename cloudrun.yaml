apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: openmoneyweb
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "2"
        run.googleapis.com/cloudsql-instances: openmoneyweb:northamerica-northeast1:openmoney-db
    spec:
      containers:
        - image: gcr.io/openmoneyweb/openmoneyweb:latest
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: db-connection-string
                  key: latest
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
